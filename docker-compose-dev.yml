version: "3"
services:
    apitest:
        build:
            context: .
            dockerfile: Dockerfile.dev
        ports:
            - "80:80"
        #volumes: 
        #    - /node_modules
        #    - .:/


# BUILD
# sudo docker-compose up --build